export type WorldPropKind='tea_station'|'book_clue'|'lantern_hook'|'garden_water';
export type WorldPropInteraction={residentId:string;kind:WorldPropKind;locationKey:string;x:number;y:number;label:string;state:'idle'|'active'|'reactive';};
export function propInteractionFor(residentId:string,activity:string,locationKey:string):WorldPropInteraction|null{if(residentId==='ren'&&(activity.includes('tea')||activity.includes('café')))return{residentId,kind:'tea_station',locationKey,x:50,y:42,label:'A fresh cup of tea',state:'active'};if(residentId==='emi'&&(activity.includes('book')||activity.includes('reading')||activity.includes('story')))return{residentId,kind:'book_clue',locationKey,x:65,y:27,label:'An open book catches the light',state:'reactive'};if(residentId==='kenji'&&(activity.includes('stall')||activity.includes('lantern')||activity.includes('calling')))return{residentId,kind:'lantern_hook',locationKey,x:76,y:57,label:'A lantern sways in the breeze',state:'active'};if(residentId==='kenji'&&activity.includes('garden'))return{residentId,kind:'garden_water',locationKey,x:39,y:72,label:'Water ripples beside the path',state:'reactive'};return null;}
