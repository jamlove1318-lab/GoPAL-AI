import type { WorldObjectDefinition } from './livingWorldObjects';
import { buildWorldLocation } from './livingWorldLocationFactory';
import type { WorldLocationDefinition } from './livingWorldLocationSchema';
import { findNearestObjectInteraction, type WorldInteractionDefinition } from './livingWorldInteraction';
import { getAvailableWorldActions, type WorldActionId } from './livingWorldActionSystem';
import { WorldActionExecutor, type WorldActionResult } from './livingWorldActionExecutor';
import { createWorldEvent, WorldEventBus } from './livingWorldEvents';
import { clampWorldPoint, resolveMovement } from '../geometry/livingWorldGeometry';
import { LivingWorldSimulation, type LivingWorldSimulationSnapshot } from './livingWorldSimulation';
export type WorldRuntimeSnapshot={locationId:string;player:{x:number;y:number};objects:WorldObjectDefinition[];nearby:WorldInteractionDefinition|null;simulation:LivingWorldSimulationSnapshot};
export type WorldInteractionState={interaction:WorldInteractionDefinition|null;distance:number|null;object:WorldObjectDefinition|null};
export class LivingWorldRuntime{readonly events=new WorldEventBus();readonly actions=new WorldActionExecutor(this.events);private location:WorldLocationDefinition;private player={x:50,y:62};private simulation:LivingWorldSimulation;private lastSimulationTime=Date.now();private running=true;
constructor(locationId='emerald-village'){this.location=buildWorldLocation(locationId);this.simulation=new LivingWorldSimulation(locationId,1,this.location.objects);this.spawnPlayer();}
loadLocation(locationId:string){this.location=buildWorldLocation(locationId);this.simulation=new LivingWorldSimulation(locationId,1,this.location.objects);this.lastSimulationTime=Date.now();this.spawnPlayer();}
getLocation(){return this.location;}getObjects(){return this.location.objects;}getPlayer(){return{...this.player};}getSimulation(){return this.simulation;}isRunning(){return this.running;}pause(){this.running=false;}resume(){this.running=true;this.lastSimulationTime=Date.now();}
tick(now=Date.now()){if(!this.running){this.lastSimulationTime=now;return this.simulation.snapshot(now);}const delta=Math.max(0,now-this.lastSimulationTime);this.lastSimulationTime=now;const simulation=this.simulation.step(delta,now);this.events.emit(createWorldEvent({type:'world-state-changed',locationId:this.location.id,payload:{simulation}}));return simulation;}
setPlayerPosition(x:number,y:number){const bounds=this.location.rules?.walkableBounds;this.player=clampWorldPoint({x,y},bounds?.minX??6,bounds?.maxX??94,bounds?.minY??12,bounds?.maxY??91);}
movePlayer(dx:number,dy:number){const desired={x:this.player.x+dx,y:this.player.y+dy};const solidObjects=this.location.objects.filter(o=>o.collision?.enabled&&o.collision.solid!==false).map(o=>({x:o.transform.x,y:o.transform.y,width:o.collision?.width??0,height:o.collision?.height??0,padding:o.collision?.padding??0}));const resolved=resolveMovement(this.player,desired,solidObjects);this.setPlayerPosition(resolved.x,resolved.y);return this.getPlayer();}
getInteractionState():WorldInteractionState{const nearest=findNearestObjectInteraction(this.player,this.location.objects);if(!nearest)return{interaction:null,distance:null,object:null};const object=this.location.objects.find(item=>item.id===nearest.interaction.targetId)??null;if(!object)return{interaction:null,distance:null,object:null};const actions=getAvailableWorldActions({object,distance:nearest.distance,unlocked:object.state?.unlocked!==false}).filter(a=>!this.isActionConsumed(object,a.id));return{interaction:{...nearest.interaction,actions:actions.map(a=>a.id)},distance:nearest.distance,object};}
getNearbyInteraction(){return this.getInteractionState().interaction;}getAvailableActions():WorldActionId[]{return this.getInteractionState().interaction?.actions??[];}
interact(action:WorldActionId,actorId='player'):WorldActionResult|null{const state=this.getInteractionState();if(!state.object||state.distance===null||!state.interaction?.actions.includes(action))return null;const result=this.actions.execute(this.location.id,state.object,action,actorId);if(!result.handled)return result;if(Object.keys(result.stateChanges).length>0){const previousState={...(state.object.state??{})};const nextState={...previousState,...result.stateChanges};this.location={...this.location,objects:this.location.objects.map(o=>o.id===state.object!.id?{...o,state:nextState}:o)};this.events.emit(createWorldEvent({type:'world-state-changed',locationId:this.location.id,objectId:state.object.id,action,actorId,payload:{previousState,stateChanges:{...result.stateChanges},nextState}}));}return result;}
snapshot():WorldRuntimeSnapshot{return{locationId:this.location.id,player:{...this.player},objects:this.location.objects.map(o=>({...o,transform:{...o.transform},visual:o.visual?{...o.visual}:undefined,collision:o.collision?{...o.collision}:undefined,interaction:o.interaction?{...o.interaction,actions:o.interaction.actions?[...o.interaction.actions]:undefined}:undefined,behavior:o.behavior?{...o.behavior}:undefined,state:o.state?{...o.state}:undefined,tags:o.tags?[...o.tags]:undefined,metadata:o.metadata?{...o.metadata}:undefined})),nearby:this.getNearbyInteraction(),simulation:this.simulation.snapshot()};}
private isActionConsumed(object:WorldObjectDefinition,action:WorldActionId){if(action==='collect')return object.state?.collected===true;if(action==='discover')return object.state?.discovered===true;if(action==='save')return object.state?.saved===true;return false;}
private spawnPlayer(){const spawn=this.location.objects.find(o=>o.category==='gameplay'&&o.type==='spawn');if(spawn)this.setPlayerPosition(spawn.transform.x,spawn.transform.y);else this.setPlayerPosition(50,62);}}
export function createLivingWorldRuntime(locationId?:string){return new LivingWorldRuntime(locationId);}
