import type { WorldObjectDefinition } from './livingWorldObjects';
import { buildWorldLocation } from './livingWorldLocationFactory';
import type { WorldLocationDefinition } from './livingWorldLocationSchema';
import { findNearestObjectInteraction, type WorldInteractionDefinition } from './livingWorldInteraction';
import { getAvailableWorldActions, type WorldActionId } from './livingWorldActionSystem';
import { WorldActionExecutor, type WorldActionResult } from './livingWorldActionExecutor';
import { createWorldEvent, WorldEventBus, type WorldEvent } from './livingWorldEvents';
import { clampWorldPoint, resolveMovement } from '../geometry/livingWorldGeometry';
import { LivingWorldSimulation, type LivingWorldSimulationSnapshot } from './livingWorldSimulation';
import { LivingWorldGameplayEngine, type GameplayState } from './livingWorldGameplayEngine';
import { LivingWorldQuestEngine, type WorldQuestState } from './livingWorldQuestEngine';
import { getLocationGameplay } from './livingWorldGameplay';
import { LivingWorldLearningEngine, type WorldLearningState } from './livingWorldLearning';
import { LivingWorldGameEngine, type WorldGameState } from './livingWorldGameEngine';
import { getLanguageWorld, getLanguageWorldForLocation, type LanguageWorldDefinition } from './livingLanguageWorlds';
export type WorldRuntimeSnapshot={locationId:string;languageWorldId:string|null;language:string|null;locale:string|null;player:{x:number;y:number};objects:WorldObjectDefinition[];nearby:WorldInteractionDefinition|null;simulation:LivingWorldSimulationSnapshot;running:boolean;gameplay:GameplayState[];quests:WorldQuestState[];learning:WorldLearningState[];games:WorldGameState[]};
export type WorldRuntimeSaveData={version:1;locationId:string;languageWorldId:string|null;player:{x:number;y:number};objects:WorldObjectDefinition[];gameplay:GameplayState[];quests:WorldQuestState[];learning:WorldLearningState[];games:WorldGameState[];savedAt:number};
export type WorldInteractionState={interaction:WorldInteractionDefinition|null;distance:number|null;object:WorldObjectDefinition|null};
export class LivingWorldRuntime{readonly events=new WorldEventBus();readonly actions=new WorldActionExecutor(this.events);private location:WorldLocationDefinition;private player={x:50,y:62};private simulation!:LivingWorldSimulation;private gameplay!:LivingWorldGameplayEngine;private quests!:LivingWorldQuestEngine;private learning!:LivingWorldLearningEngine;private games!:LivingWorldGameEngine;private lastSimulationTime=Date.now();private running=true;private unsubscribeEvents!:()=>void;constructor(locationId='emerald-village'){this.location=buildWorldLocation(locationId);this.initialize(locationId);this.unsubscribeEvents=this.events.subscribe(event=>this.handleCanonicalEvent(event));this.simulation=new LivingWorldSimulation(locationId,1,this.location.objects,this.events);this.spawnPlayer();}private initialize(locationId:string){this.gameplay=new LivingWorldGameplayEngine(this.events,locationId,getLocationGameplay(locationId));this.quests=new LivingWorldQuestEngine(this.events,locationId);this.learning=new LivingWorldLearningEngine(this.events,locationId,this.location.objects);this.games=new LivingWorldGameEngine(this.events,locationId,getLocationGameplay(locationId));}loadLocation(locationId:string,languageWorldId?:string){this.stop();this.location=buildWorldLocation(locationId);if(languageWorldId){const world=getLanguageWorld(languageWorldId);if(world)this.location={...this.location,worldKind:world.homeWorld?'home':'language',languageWorldId:world.id,language:world.language,locale:world.locale,metadata:{...(this.location.metadata??{}),languageWorldId:world.id,language:world.language,locale:world.locale,worldDisplayName:world.name}};}this.initialize(locationId);this.simulation=new LivingWorldSimulation(locationId,1,this.location.objects,this.events);this.lastSimulationTime=Date.now();this.spawnPlayer();this.start();}transitionToLocation(locationId:string,actorId='player',languageWorldId?:string){if(locationId===this.location.id)return false;this.events.emit(createWorldEvent({type:'travel-requested',locationId:this.location.id,actorId,payload:{fromLocationId:this.location.id,toLocationId:locationId,toLanguageWorldId:languageWorldId}}));this.loadLocation(locationId,languageWorldId);return true;}transitionToLanguageLocation(locationId:string,languageWorldId:string,actorId='player'){return this.transitionToLocation(locationId,actorId,languageWorldId);}start(now=Date.now()){this.running=true;this.lastSimulationTime=now;}stop(){this.running=false;}pause(){this.stop();}resume(now=Date.now()){this.start(now);}isRunning(){return this.running;}getLocation(){return this.location;}getLanguageWorld():LanguageWorldDefinition|null{return getLanguageWorldForLocation(this.location);}getObjects(){return this.location.objects;}getPlayer(){return{...this.player};}getSimulation(){return this.simulation;}getGameplay(){return this.gameplay;}getQuests(){return this.quests;}getLearning(){return this.learning;}getGames(){return this.games;}tick(now=Date.now()){if(!this.running)return this.simulation.snapshot(now);const delta=Math.max(0,now-this.lastSimulationTime);this.lastSimulationTime=now;const simulation=this.simulation.step(delta,now);this.events.emit(createWorldEvent({type:'world-state-changed',locationId:this.location.id,payload:{simulation}}));return simulation;}setPlayerPosition(x:number,y:number){const bounds=this.location.rules?.walkableBounds;this.player=clampWorldPoint({x,y},bounds?.minX??6,bounds?.maxX??94,bounds?.minY??12,bounds?.maxY??91);}movePlayer(dx:number,dy:number){const desired={x:this.player.x+dx,y:this.player.y+dy};const solidObjects=this.location.objects.filter(o=>o.collision?.enabled&&o.collision.solid!==false).map(o=>({x:o.transform.x,y:o.transform.y,width:o.collision?.width??0,height:o.collision?.height??0,padding:o.collision?.padding??0}));const resolved=resolveMovement(this.player,desired,solidObjects);this.setPlayerPosition(resolved.x,resolved.y);return this.getPlayer();}getInteractionState():WorldInteractionState{const nearest=findNearestObjectInteraction(this.player,this.location.objects);if(!nearest)return{interaction:null,distance:null,object:null};const object=this.location.objects.find(item=>item.id===nearest.interaction.targetId)??null;if(!object)return{interaction:null,distance:null,object:null};const actions=getAvailableWorldActions({object,distance:nearest.distance,unlocked:object.state?.unlocked!==false}).filter(a=>!this.isActionConsumed(object,a.id));return{interaction:{...nearest.interaction,actions:actions.map(a=>a.id)},distance:nearest.distance,object};}getNearbyInteraction(){return this.getInteractionState().interaction;}getAvailableActions():WorldActionId[]{return this.getInteractionState().interaction?.actions??[];}interact(action:WorldActionId,actorId='player'):WorldActionResult|null{const state=this.getInteractionState();if(!state.object||state.distance===null||!state.interaction?.actions.includes(action))return null;const gameplayObject=getLocationGameplay(this.location.id).find(item=>item.id===state.object!.id);if(gameplayObject)this.gameplay.interact(gameplayObject,action,actorId);const result=this.actions.execute(this.location.id,state.object,action,actorId);if(!result.handled)return result;if(Object.keys(result.stateChanges).length>0){const previousState={...(state.object.state??{})};const nextState={...previousState,...result.stateChanges};this.location={...this.location,objects:this.location.objects.map(o=>o.id===state.object!.id?{...o,state:nextState}:o)};this.events.emit(createWorldEvent({type:'world-state-changed',locationId:this.location.id,objectId:state.object.id,action,actorId,payload:{previousState,stateChanges:{...result.stateChanges},nextState}}));}return result;}completeLearning(activityId:string,actorId='player',success=true){return this.learning.complete(activityId,actorId,success);}startGame(gameId?:string,actorId='player'){return this.games.start(gameId,actorId);}completeGame(gameId:string,score=0,actorId='player'){return this.games.complete(gameId,score,actorId);}save():WorldRuntimeSaveData{return{version:1,locationId:this.location.id,languageWorldId:getLanguageWorldForLocation(this.location)?.id??this.location.languageWorldId??null,player:{...this.player},objects:this.location.objects.map(o=>({...o,state:o.state?{...o.state}:undefined})),gameplay:this.gameplay.getAllStates().map(s=>({...s})),quests:this.quests.getAllStates().map(s=>({...s,objectives:s.objectives.map(o=>({...o}))})),learning:this.learning.getAllStates().map(s=>({...s})),games:this.games.getAllStates().map(s=>({...s})),savedAt:Date.now()};}restore(data:WorldRuntimeSaveData){if(!data||data.version!==1)return false;this.loadLocation(data.locationId,data.languageWorldId??undefined);const objectStates=new Map(data.objects.map(o=>[o.id,o.state]));this.location={...this.location,objects:this.location.objects.map(o=>objectStates.has(o.id)?{...o,state:objectStates.get(o.id)}:o)};this.gameplay.restore(data.gameplay);this.quests.restore(data.quests);this.learning.restore(data.learning);this.games.restore(data.games);this.setPlayerPosition(data.player.x,data.player.y);return true;}snapshot():WorldRuntimeSnapshot{const world=this.getLanguageWorld();return{locationId:this.location.id,languageWorldId:world?.id??this.location.languageWorldId??null,language:world?.language??this.location.language??null,locale:world?.locale??this.location.locale??null,player:{...this.player},objects:this.location.objects.map(o=>({...o,transform:{...o.transform},visual:o.visual?{...o.visual}:undefined,collision:o.collision?{...o.collision}:undefined,interaction:o.interaction?{...o.interaction,actions:o.interaction.actions?[...o.interaction.actions]:undefined}:undefined,behavior:o.behavior?{...o.behavior}:undefined,state:o.state?{...o.state}:undefined,tags:o.tags?[...o.tags]:undefined,metadata:o.metadata?{...o.metadata}:undefined})),nearby:this.getNearbyInteraction(),simulation:this.simulation.snapshot(),running:this.running,gameplay:this.gameplay.getAllStates(),quests:this.quests.getAllStates(),learning:this.learning.getAllStates(),games:this.games.getAllStates()};}private handleCanonicalEvent(event:WorldEvent){if(event.type==='world-state-changed')return;this.quests.handleEvent(event);this.learning.handleEvent(event);this.games.handleEvent(event);}private isActionConsumed(object:WorldObjectDefinition,action:WorldActionId){if(action==='collect')return object.state?.collected===true;if(action==='discover')return object.state?.discovered===true;if(action==='save')return object.state?.saved===true;return false;}private spawnPlayer(){const spawn=this.location.objects.find(o=>o.category==='gameplay'&&o.type==='spawn');if(spawn)this.setPlayerPosition(spawn.transform.x,spawn.transform.y);else this.setPlayerPosition(50,62);}dispose(){this.stop();this.unsubscribeEvents();this.events.clear();}}
export function createLivingWorldRuntime(locationId?:string){return new LivingWorldRuntime(locationId);}
