import type { WorldActionId } from './livingWorldActionSystem';
import type { WorldEvent, WorldEventBus } from './livingWorldEvents';
import type { WorldObjectDefinition } from './livingWorldObjects';
export type WorldLearningKind='lesson'|'vocabulary'|'grammar'|'conversation'|'discovery'|'quest'|'school'|'library'|'market'|'cafe';
export type WorldLearningActivity={id:string;kind:WorldLearningKind;locationId:string;objectId:string;title:string;description:string;xp:number;tags:string[];prerequisiteIds:string[];context:{objectType?:string;characterRole?:string;language?:string;locale?:string}};
export type WorldLearningState={activityId:string;started:boolean;completed:boolean;completionCount:number;lastEventAt?:number};
const XP:Record<WorldLearningKind,number>={lesson:20,vocabulary:10,grammar:15,conversation:20,discovery:8,quest:25,school:20,library:15,market:12,cafe:15};
function activityForObject(locationId:string,object:WorldObjectDefinition):WorldLearningActivity|null{const type=String(object.type??'').toLowerCase();const tags=new Set((object.tags??[]).map(String));let kind:WorldLearningKind|null=null;if(type==='school'||type==='academy'||tags.has('school'))kind='school';else if(type==='library'||tags.has('library'))kind='library';else if(type==='market'||type==='shop'||tags.has('market'))kind='market';else if(type==='cafe'||type==='restaurant'||tags.has('conversation'))kind='cafe';else if(type==='lesson-point'||tags.has('lesson'))kind='lesson';else if(type==='dialogue-point'||tags.has('conversation'))kind='conversation';else if(type==='quest-marker'||tags.has('quest'))kind='quest';else if(type==='landmark'||tags.has('discovery'))kind='discovery';else if(tags.has('grammar'))kind='grammar';else if(tags.has('vocabulary'))kind='vocabulary';if(!kind)return null;const language=typeof object.metadata?.language==='string'?object.metadata.language:undefined;const locale=typeof object.metadata?.locale==='string'?object.metadata.locale:undefined;const title=typeof object.metadata?.label==='string'?object.metadata.label:String(object.type);return{id:`learning:${locationId}:${object.id}:${kind}`,kind,locationId,objectId:object.id,title,description:`Learn through ${title.toLowerCase()} in the living world.`,xp:XP[kind],tags:[...(object.tags??[]),kind],prerequisiteIds:[],context:{objectType:type,language,locale}};}
export function getWorldLearningActivities(locationId:string,objects:WorldObjectDefinition[]){return objects.map(object=>activityForObject(locationId,object)).filter((item):item is WorldLearningActivity=>!!item);}
export class LivingWorldLearningEngine{private readonly activities:WorldLearningActivity[];private readonly states=new Map<string,WorldLearningState>();constructor(private readonly events:WorldEventBus,private readonly locationId:string,objects:WorldObjectDefinition[]){this.activities=getWorldLearningActivities(locationId,objects);for(const activity of this.activities)this.states.set(activity.id,{activityId:activity.id,started:false,completed:false,completionCount:0});}
 getActivities(){return[...this.activities];}getState(id:string){return this.states.get(id)??null;}getAllStates(){return[...this.states.values()];}findForObject(id:string){return this.activities.filter(activity=>activity.objectId===id);}
 start(id:string,actorId='player'){const activity=this.activities.find(item=>item.id===id);if(!activity)return null;const state=this.states.get(id)??{activityId:id,started:false,completed:false,completionCount:0};if(state.completed)return{activity,state};state.started=true;this.states.set(id,state);this.events.emit({id:`learning-start-${Date.now()}-${id}`,type:'activity-started',timestamp:Date.now(),locationId:this.locationId,objectId:activity.objectId,actorId,payload:{activityId:id,kind:activity.kind,xp:activity.xp}});return{activity,state};}
 complete(id:string,actorId='player',success=true){const activity=this.activities.find(item=>item.id===id);if(!activity||!success)return null;const state=this.states.get(id)??{activityId:id,started:false,completed:false,completionCount:0};if(state.completed)return{activity,state,xp:0};state.started=true;state.completed=true;state.completionCount=1;state.lastEventAt=Date.now();this.states.set(id,state);this.events.emit({id:`learning-complete-${Date.now()}-${id}`,type:'activity-completed',timestamp:Date.now(),locationId:this.locationId,objectId:activity.objectId,actorId,action:'learn' as WorldActionId,payload:{activityId:id,kind:activity.kind,xp:activity.xp}});this.events.emit({id:`learning-xp-${Date.now()}-${id}`,type:'learning-xp-awarded',timestamp:Date.now(),locationId:this.locationId,objectId:activity.objectId,actorId,action:'learn' as WorldActionId,payload:{activityId:id,xp:activity.xp}});if(activity.kind==='vocabulary')this.events.emit({id:`learning-vocab-${Date.now()}-${id}`,type:'vocabulary-learned',timestamp:Date.now(),locationId:this.locationId,objectId:activity.objectId,actorId,action:'learn',payload:{activityId:id}});if(activity.kind==='grammar')this.events.emit({id:`learning-grammar-${Date.now()}-${id}`,type:'grammar-practiced',timestamp:Date.now(),locationId:this.locationId,objectId:activity.objectId,actorId,action:'learn',payload:{activityId:id}});return{activity,state,xp:activity.xp};}
 handleEvent(event:WorldEvent){if(event.locationId!==this.locationId||!event.objectId)return null;const activity=this.activities.find(item=>item.objectId===event.objectId);if(!activity)return null;if(event.type==='dialogue-started'||event.action==='learn')return this.start(activity.id,event.actorId);return null;}
 restore(states:WorldLearningState[]){for(const state of states)if(this.states.has(state.activityId))this.states.set(state.activityId,{...state});}}
