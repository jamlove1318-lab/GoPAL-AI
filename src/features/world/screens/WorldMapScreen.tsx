import React, { useEffect, useMemo, useState } from 'react';
import { View, Text, ScrollView, Pressable, Animated, Easing } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useWorldState } from '../../../hooks/useWorldState';
import { LocalStore, CulturalArtifact, RevisitRecord } from '../../../lib/localStore';
import { WaveStore, LivingObject } from '../../../lib/waveStore';
import { eventBus } from '../../../engines/events/eventBus';
import { WonderPromptModal } from '../../learning/components/WonderPromptModal';
import { WorldThresholdModal } from '../components/WorldThresholdModal';
import { EmaRitualModal } from '../components/EmaRitualModal';
import { Sparkles, Compass, Eye } from 'lucide-react-native';

interface WorldMapScreenProps { onStartScenario?: (scenarioKey: string) => void; }
interface LocationDetail { id: string; key: string; name: string; description: string; npcName: string; npcRole: string; familiarity_stage: 'personal'|'familiar'|'discovered'|'unknown'; scenarioKey?: string; icon: string; }
const LOCATION_METADATA: Record<string, {description:string;npcName:string;npcRole:string;scenarioKey?:string;icon:string}> = {
  study_room:{description:'Your cozy personal hub. Desk, growing bonsai plant, radio, and notes.',npcName:'Cassidy',npcRole:'Companion & Guide',icon:'study'},
  cozy_cafe:{description:'A warm café with gentle rain sounds, aroma of matcha and roasted coffee.',npcName:'Ren',npcRole:'Friendly Barista',scenarioKey:'scen-cafe-order',icon:'cafe'},
  whispering_library:{description:'Silent archives filled with ancient scrolls and cultural manuscripts.',npcName:'Emi',npcRole:'Wisdom Keeper & Historian',scenarioKey:'scen-library-inquiry',icon:'library'},
  lantern_market:{description:'Bustling night alley lit by red lanterns with street food vendors.',npcName:'Kenji',npcRole:'Stall Master',scenarioKey:'scen-market-browse',icon:'market'},
  zen_garden:{description:'Tranquil raked gravel and bamboo groves for deep reflection.',npcName:'Master Jin',npcRole:'Garden Monk',icon:'garden'},
};
const ICONS: Record<string,string>={study:'📚',cafe:'☕',library:'📜',market:'🏮',garden:'🎋'};
export function WorldMapScreen({onStartScenario}:WorldMapScreenProps){
  const {state,changeLocation,worldEngine}=useWorldState();
  const [locations,setLocations]=useState<LocationDetail[]>([]),[artifacts,setArtifacts]=useState<CulturalArtifact[]>([]),[revisitStats,setRevisitStats]=useState<Record<string,RevisitRecord>>({}),[revisitNotes,setRevisitNotes]=useState<Record<string,string|null>>({}),[gatedByLoc,setGatedByLoc]=useState<Record<string,number>>({}),[living,setLiving]=useState<LivingObject[]>([]);
  const [selectedArtifact,setSelectedArtifact]=useState<CulturalArtifact|null>(null),[thresholdVisible,setThresholdVisible]=useState(false),[pendingTargetLocation,setPendingTargetLocation]=useState<LocationDetail|null>(null),[showEmaModal,setShowEmaModal]=useState(false);
  const pulse=useState(()=>new Animated.Value(0))[0];
  const loadData=async()=>{const raw=await worldEngine.listLocations('world-emerald-valley');const arts=await LocalStore.getCulturalArtifacts();const stats=await LocalStore.getRevisitStats();setArtifacts(arts);setRevisitStats(stats);const notes:Record<string,string|null>={};for(const loc of raw)notes[loc.key]=(await worldEngine.getRevisitDifference(loc.key)).note;setRevisitNotes(notes);const nodes=await LocalStore.getKnowledgeNodes();const gated:Record<string,number>={};for(const n of nodes)if(n.masteryLevel<40)gated[n.locationKey]=(gated[n.locationKey]||0)+1;setGatedByLoc(gated);setLiving(await WaveStore.getLivingObjects());setLocations(raw.map(loc=>{const meta=LOCATION_METADATA[loc.key]||{description:'An unexplored region in the valley.',npcName:'Resident',npcRole:'Local',icon:'map'};return {...loc,description:meta.description,npcName:meta.npcName,npcRole:meta.npcRole,scenarioKey:meta.scenarioKey,icon:meta.icon,familiarity_stage:loc.familiarity_stage as LocationDetail['familiarity_stage']};}));};
  useEffect(()=>{void loadData();const off=eventBus.on('world:returned',()=>void loadData());return off;},[]);
  useEffect(()=>{const loop=Animated.loop(Animated.sequence([Animated.timing(pulse,{toValue:1,duration:1800,easing:Easing.inOut(Easing.sin),useNativeDriver:true}),Animated.timing(pulse,{toValue:0,duration:1800,easing:Easing.inOut(Easing.sin),useNativeDriver:true})]));loop.start();return()=>loop.stop();},[pulse]);
  const growth=useMemo(()=>living.reduce((sum,o)=>sum+o.growth,0),[living]);
  const handleTravelRequest=(target:LocationDetail)=>{setPendingTargetLocation(target);setThresholdVisible(true);};
  const handleThresholdComplete=async()=>{if(!pendingTargetLocation)return;await changeLocation(pendingTargetLocation.id);await LocalStore.recordLocationVisit(pendingTargetLocation.key);setThresholdVisible(false);setPendingTargetLocation(null);await loadData();};
  const familiarity=(s:string)=>s==='personal'?['Personal Sanctuary','text-emerald-400','bg-emerald-950/40 border-emerald-500/40']:s==='familiar'?['Familiar Ground','text-blue-400','bg-blue-950/40 border-blue-500/40']:s==='discovered'?['Discovered','text-amber-400','bg-amber-950/40 border-amber-500/40']:['Undiscovered Lore','text-slate-400','bg-slate-900 border-slate-700'];
  return <SafeAreaView className="flex-1 bg-transparent"><ScrollView className="flex-1 px-5 pt-3" showsVerticalScrollIndicator={false} contentContainerStyle={{paddingBottom:60}}>
    <View className="flex-row items-center justify-between"><View><Text className="text-xs font-semibold uppercase tracking-wider text-emerald-400">Emerald Valley World Map</Text><Text className="text-2xl font-bold text-white">Locations & Lore</Text></View><View className="rounded-full bg-slate-800/80 px-3 py-1 border border-slate-700"><Text className="text-xs font-medium text-slate-300">{locations.length} Known Regions</Text></View></View>
    <Text className="mt-2 text-xs leading-relaxed text-slate-400">Explore living locations across Emerald Valley. Each place has its own resident NPCs, atmosphere, and cultural artifacts.</Text>
    <Animated.View style={{opacity:pulse.interpolate({inputRange:[0,1],outputRange:[.78,1]})}} className="mt-4 rounded-2xl border border-emerald-500/25 bg-emerald-950/25 p-3"><View className="flex-row items-center justify-between"><View><Text className="text-[10px] font-bold uppercase tracking-wider text-emerald-300">The valley remembers</Text><Text className="mt-0.5 text-xs text-slate-300">Living things have changed since your last visit.</Text></View><Text className="text-lg">🌿</Text></View><Text className="mt-2 text-[10px] text-slate-500">Living growth · {growth}</Text></Animated.View>
    <Pressable onPress={()=>setShowEmaModal(true)} className="mt-3 flex-row items-center justify-between rounded-2xl border border-amber-500/40 bg-amber-950/20 p-3.5 active:bg-amber-950/40"><View className="flex-row items-center gap-2.5"><Text className="text-2xl">🎋</Text><View><Text className="text-xs font-bold text-amber-300">Ema Wish Hanging (絵馬)</Text><Text className="text-[10px] text-slate-300">Inscribe a learning goal at Zen Garden</Text></View></View><Text className="text-[10px] font-bold text-amber-300">Ritual ✨</Text></Pressable>
    <View className="mt-4 gap-4">{locations.map(loc=>{const isCurrent=state?.location?.id===loc.id,isLocked=loc.familiarity_stage==='unknown',fam=familiarity(loc.familiarity_stage),locArtifacts=artifacts.filter(a=>a.locationKey===loc.key),visits=revisitStats[loc.key]?.count||0;return <View key={loc.id} className={`rounded-2xl border p-4 shadow-lg ${isCurrent?'border-emerald-500/60 bg-emerald-950/20':isLocked?'border-slate-800/40 bg-slate-900/40 opacity-70':'border-slate-800 bg-slate-900'}`}><View className="flex-row items-start justify-between"><View className="flex-row items-center gap-3"><View className="h-12 w-12 items-center justify-center rounded-2xl bg-slate-800/80 border border-slate-700"><Text className="text-2xl">{ICONS[loc.key]||'📍'}</Text></View><View><Text className="text-base font-bold text-white">{loc.name}</Text><Text className="text-xs text-indigo-300">{loc.npcName} · {loc.npcRole}</Text></View></View><View className="items-end gap-1"><View className={`rounded-full px-2.5 py-0.5 border ${fam[2]}`}><Text className={`text-[10px] font-bold ${fam[1]}`}>{fam[0]}</Text></View>{visits>0&&<Text className="text-[9px] text-slate-500 font-medium">Visited {visits} {visits===1?'time':'times'}</Text>}</View></View><Text className="mt-3 text-xs leading-relaxed text-slate-300">{loc.description}</Text>{revisitNotes[loc.key]&&<View className="mt-2 rounded-xl bg-indigo-950/30 border border-indigo-500/20 p-2.5"><Text className="text-[11px] italic text-indigo-200">“{revisitNotes[loc.key]}”</Text></View>}{gatedByLoc[loc.key]>0&&<View className="mt-2 flex-row items-center gap-1.5 rounded-xl bg-slate-800/60 border border-slate-700 p-2.5"><Eye size={12} color="#94a3b8"/><Text className="text-[10px] text-slate-400">A sign here is still unreadable — keep learning to reveal {gatedByLoc[loc.key]} hidden {gatedByLoc[loc.key]===1?'concept':'concepts'}.</Text></View>}{locArtifacts.length>0&&<View className="mt-3 rounded-xl bg-slate-950/60 p-3 border border-slate-800"><View className="flex-row items-center gap-1.5 mb-1.5"><Sparkles size={12} color="#fbbf24"/><Text className="text-[11px] font-bold text-amber-300">Cultural Keepsakes & Wonder Prompts</Text></View>{locArtifacts.map(art=><Pressable key={art.id} onPress={()=>setSelectedArtifact(art)} className="flex-row items-center justify-between rounded-lg bg-amber-950/30 p-2 border border-amber-500/20 active:bg-amber-950/50"><Text className="text-[10px] font-bold text-amber-200">{art.name} ({art.japaneseName})</Text><Text className="text-[9px] text-amber-400 font-semibold">Inspect 🔍</Text></Pressable>)}</View>}{!isLocked&&<View className="mt-3.5 flex-row gap-2">{!isCurrent&&<Pressable className="flex-1 flex-row items-center justify-center gap-1.5 rounded-xl bg-slate-800 py-2 active:bg-slate-700" onPress={()=>handleTravelRequest(loc)}><Compass size={14} color="#38bdf8"/><Text className="text-xs font-semibold text-slate-200">Travel Here</Text></Pressable>}{loc.scenarioKey&&<Pressable className="flex-1 flex-row items-center justify-center gap-1.5 rounded-xl bg-emerald-500 py-2 active:bg-emerald-600" onPress={()=>onStartScenario?.(loc.scenarioKey!)}><Sparkles size={14} color="#064e3b"/><Text className="text-xs font-bold text-emerald-950">Practice with {loc.npcName}</Text></Pressable>}</View>}</View>})}</View>
  </ScrollView><WorldThresholdModal visible={thresholdVisible} fromLocationName={state?.location?.name??'Current Location'} toLocationName={pendingTargetLocation?.name??'Destination'} toLocationKey={pendingTargetLocation?.key??'study_room'} onTransitionComplete={handleThresholdComplete}/><WonderPromptModal visible={selectedArtifact!==null} artifact={selectedArtifact} onClose={()=>setSelectedArtifact(null)}/><EmaRitualModal visible={showEmaModal} onClose={()=>setShowEmaModal(false)}/></SafeAreaView>;
}