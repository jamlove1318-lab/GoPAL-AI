import React,{useEffect,useMemo,useState}from'react';
import{Modal,Pressable,ScrollView,Text,TextInput,View}from'react-native';
import{X,Volume2,Sparkles,Check,ArrowRight}from'lucide-react-native';
import{resolveLanguageWorld}from'../../../engines/world/languageWorldEngine';import{getDestinationResidents}from'../../../engines/world/destinationResidentEngine';import{contextualLanguageLearningEngine}from'../../../engines/learning/contextualLanguageLearningEngine';import{worldLearningScenarioEngine}from'../../../engines/learning/worldLearningScenarioEngine';import{languageCapabilityEngine,ScenarioCapability}from'../../../engines/learning/languageCapabilityEngine';import{worldLearningFlowEngine}from'../../../engines/learning/worldLearningFlowEngine';import{residentConversationContinuityEngine,ResidentConversationState}from'../../../engines/learning/residentConversationContinuityEngine';import{worldLearningResponseEngine}from'../../../engines/learning/worldLearningResponseEngine';import{auth}from'../../../services/auth';import{WordBankAnswerBuilder}from'../components/WordBankAnswerBuilder';import{WorldLivingInputBar}from'../components/WorldLivingInputBar';
const LEGACY:Record<string,string>={'scen-cafe-order':'ja-market-first-purchase','scen-library-inquiry':'ja-market-first-purchase','scen-market-browse':'ja-market-first-purchase'};const resolveScenario=(key:string)=>worldLearningScenarioEngine.all().find(item=>item.id===(LEGACY[key]??key));export function isWorldLearningScenario(key:string){return Boolean(resolveScenario(key));}
export function WorldLearningMomentModal({visible,scenarioKey,onClose,onComplete,onResponse}:{visible:boolean;scenarioKey:string;onClose:()=>void;onComplete?:()=>void;onResponse?:(input:'typed'|'spoken'|'guided',outcome:'success'|'confusion')=>void}){
 const scenario=resolveScenario(scenarioKey);const[phase,setPhase]=useState<'scene'|'learn'|'respond'|'result'>('scene');const[selected,setSelected]=useState<number|null>(null);const[capability,setCapability]=useState<ScenarioCapability|null>(null);const[conversation,setConversation]=useState<ResidentConversationState|null>(null);const[freeResponse,setFreeResponse]=useState('');const[responseError,setResponseError]=useState('');const[answerMode,setAnswerMode]=useState<'free'|'blocks'>('free');const[showGuidance,setShowGuidance]=useState(false);const world=useMemo(()=>scenario?resolveLanguageWorld(scenario.worldId):null,[scenario?.worldId]);const resident=useMemo(()=>{if(!scenario)return null;const residents=getDestinationResidents(scenario.worldId,scenario.placeId);const goal=scenario.goal.toLowerCase();return residents.find(item=>item.languageNeed.toLowerCase().split(' ').some(word=>word.length>3&&goal.includes(word)))??residents[0]??null;},[scenario?.worldId,scenario?.placeId,scenario?.goal]);const moment=useMemo(()=>world&&scenario?contextualLanguageLearningEngine.buildMoment(world,scenario.placeId,scenario.skill):null,[world,scenario]);const activeOptions=conversation?.responseOptions??scenario?.responseOptions??[];const activeTarget=conversation?.targetLanguage??scenario?.targetLanguage??'';const activeTranslation=conversation?.translation??scenario?.translation??'';const activeVocabulary=conversation?.vocabulary??scenario?.vocabulary??[];const wordBank=useMemo(()=>Array.from(new Set([...activeVocabulary.map(item=>item.word).filter(Boolean),...activeOptions.map(item=>item.text.split(/\s+/)).flat().filter(Boolean)])).slice(0,18),[activeVocabulary,activeOptions]);useEffect(()=>{if(visible&&scenario){setPhase('scene');setSelected(null);setFreeResponse('');setResponseError('');setAnswerMode('free');setShowGuidance(false);setConversation(residentConversationContinuityEngine.begin(scenario,resident?.id));languageCapabilityEngine.scenario(scenario.id).then(setCapability);}},[visible,scenarioKey,scenario?.id,resident?.id]);if(!visible||!scenario||!moment||!world)return null;const independent=capability?.state==='independent';const advance=()=>setPhase(p=>p==='scene'?'learn':p==='learn'?'respond':p);const finishChoice=async(choice:{text:string;correct:boolean},i:number,input:'typed'|'spoken'|'guided'='typed')=>{setSelected(i);onResponse?.(input,choice.correct?'success':'confusion');const user=await auth.getCurrentUser();if(!user)return;const isFinalTurn=!conversation?.canContinue;const result=isFinalTurn&&choice.correct?await worldLearningFlowEngine.complete({userId:user.id,scenarioId:scenario.id,success:true,residentId:resident?.id}):await worldLearningFlowEngine.completeTurn({scenarioId:scenario.id,targetLanguage:activeTarget,vocabulary:activeVocabulary.map(item=>item.word),success:choice.correct,skill:scenario.skill});if(result)setCapability(result.capability);setPhase('result');};const choose=async(i:number)=>{const choice=activeOptions[i];if(choice)await finishChoice(choice,i,'typed');};const submitResponse=async(answer:string,input:'typed'|'spoken'|'guided'='typed')=>{if(!conversation)return;const evaluated=worldLearningResponseEngine.evaluate(conversation,answer);if(!evaluated.accepted){setFreeResponse(answer);setResponseError(evaluated.reason==='empty'?'Write an answer first.':'That answer does not fit this moment yet. Try another response or Guidance.');return;}setFreeResponse(answer);setResponseError('');await finishChoice(conversation.responseOptions[evaluated.matchedOption!],evaluated.matchedOption!,input);};const continueMoment=()=>{if(!conversation?.canContinue)return;setConversation(residentConversationContinuityEngine.continue(conversation,scenario));setSelected(null);setFreeResponse('');setResponseError('');setAnswerMode('free');setShowGuidance(false);setPhase('respond');};const returnToWorld=()=>{onComplete?.();onClose()};return <Modal visible={visible} animationType="slide" transparent statusBarTranslucent><View className="flex-1 justify-end bg-transparent"><View className="max-h-[25%] rounded-t-[28px] border border-white/10 bg-slate-950/95 px-4 pb-3 pt-2"><View className="mb-1 flex-row items-center justify-between"><View className="flex-1"><Text className="text-[9px] font-bold uppercase tracking-[2px] text-emerald-300">{world.worldName} · {moment.title}</Text><Text className="mt-0.5 text-base font-black text-white">{phase==='scene'?'A moment in the world':phase==='learn'?'Notice what you need':phase==='respond'?(conversation&&conversation.turn>1?'Your turn again':'Your turn'):selected!==null&&activeOptions[selected]?.correct?'The resident understands':'Try again'}</Text></View><Pressable onPress={onClose} className="rounded-full bg-white/10 p-2"><X size={17} color="#cbd5e1"/></Pressable></View><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{paddingBottom:2}}><View className="mb-1.5 rounded-2xl border border-white/10 bg-slate-900/90 p-2.5"><Text className="text-[8px] font-bold uppercase tracking-[1.5px] text-emerald-300">{resident?.name??'Resident'}</Text><Text className="mt-1 text-sm font-semibold leading-5 text-white">{phase==='respond'?(conversation?.nextPrompt??activeTarget):phase==='result'&&selected!==null?(activeOptions[selected]?.correct?'The resident responds positively.':'The resident pauses, then gives you another chance.'):(activeTarget||scenario.scene)}</Text>{phase!=='respond'&&capability?.showTranslation!==false&&<Text className="mt-0.5 text-[11px] italic text-slate-400">{activeTranslation}</Text>}<View className="mt-1 flex-row items-center"><Volume2 size={13} color="#6ee7b7"/><Text className="ml-1 text-[9px] text-slate-500">Resident dialogue</Text></View></View>{phase==='scene'&&<View className="gap-2"><View className="rounded-2xl border border-emerald-300/10 bg-emerald-400/5 p-2.5"><Text className="text-xs leading-5 text-slate-200">{scenario.scene}</Text></View><Pressable onPress={()=>independent?setPhase('respond'):advance()} className="flex-row items-center justify-center gap-2 rounded-xl bg-emerald-500 py-2.5"><Text className="text-sm font-bold text-slate-950">{independent?'I understand':'Help me understand'}</Text><ArrowRight size={16} color="#020617"/></Pressable></View>}{phase==='learn'&&<View className="gap-2"><Text className="text-xs leading-5 text-slate-400">Look at the situation, then use these words when you are ready.</Text>{activeVocabulary.map(item=><View key={item.word} className="rounded-xl border border-white/10 bg-slate-900 p-2"><View className="flex-row items-center justify-between"><View><Text className="text-base font-black text-white">{item.word}</Text><Text className="text-[10px] text-emerald-200">{item.meaning}</Text></View><Volume2 size={15} color="#6ee7b7"/></View></View>)}<Pressable onPress={advance} className="flex-row items-center justify-center gap-2 rounded-xl bg-emerald-500 py-2.5"><Text className="text-sm font-bold text-slate-950">Talk to the resident</Text><Sparkles size={16} color="#020617"/></Pressable></View>}{phase==='respond'&&<View className="gap-2"><Text className="text-xs leading-4 text-slate-400">The resident is waiting. Choose how you want to answer.</Text><WorldLivingInputBar active={answerMode==='blocks'?'words':'type'} onSelect={mode=>{if(mode==='words'){setAnswerMode('blocks');setShowGuidance(false)}else if(mode==='type'){setAnswerMode('free');setShowGuidance(false)}else if(mode==='speak')setResponseError('Voice input is not connected yet, so type or build your answer for now.');else setShowGuidance(value=>!value)}}/><View className="mt-1">{answerMode==='free'?<View className="rounded-xl border border-emerald-300/10 bg-emerald-400/5 p-2"><TextInput value={freeResponse} onChangeText={value=>{setFreeResponse(value);setResponseError('')}} placeholder="Type what you would say..." placeholderTextColor="#64748b" autoCapitalize="none" autoCorrect={false} className="min-h-[42px] rounded-lg border border-white/10 bg-slate-950 px-3 py-1 text-sm text-white" multiline/><Pressable onPress={()=>submitResponse(freeResponse,'typed')} className="mt-1.5 rounded-lg bg-emerald-500 py-2"><Text className="text-center text-xs font-bold text-slate-950">Submit</Text></Pressable></View>:<WordBankAnswerBuilder words={wordBank} onSubmit={answer=>submitResponse(answer,'guided')}/>}</View>{showGuidance&&<View className="rounded-xl border border-emerald-300/15 bg-emerald-400/5 px-3 py-2"><Text className="text-[8px] font-bold uppercase tracking-[1.5px] text-emerald-300">Guidance</Text><Text className="mt-0.5 text-[11px] leading-4 text-slate-300">{conversation?.hint??scenario.goal}. Think about what you would naturally say to this resident in this situation.</Text></View>}{responseError&&<Text className="text-[10px] leading-4 text-amber-300">{responseError}</Text>}</View>}{phase==='result'&&selected!==null&&<View className="items-center gap-2 py-1"><View className="h-10 w-10 items-center justify-center rounded-full bg-emerald-400/15"><Check size={22} color="#6ee7b7"/></View><Text className="text-center text-xs leading-5 text-slate-300">{activeOptions[selected]?.correct?`${scenario.successWorldChange} ${conversation?.canContinue?'The conversation continues.':'Your ability in this situation is now remembered.'}`:'The resident gives you another chance. Look at the situation and try again.'}</Text>{activeOptions[selected]?.correct&&conversation?.canContinue&&<Pressable onPress={continueMoment} className="rounded-xl bg-emerald-500 px-4 py-2.5"><Text className="text-xs font-bold text-slate-950">Continue talking</Text></Pressable>}{activeOptions[selected]?.correct&&!conversation?.canContinue&&<Pressable onPress={returnToWorld} className="rounded-xl bg-emerald-500 px-4 py-2.5"><Text className="text-xs font-bold text-slate-950">Return to the world</Text></Pressable>}{!activeOptions[selected]?.correct&&<Pressable onPress={()=>{setPhase('respond');setSelected(null)}} className="rounded-xl bg-emerald-500 px-4 py-2.5"><Text className="text-xs font-bold text-slate-950">Try again</Text></Pressable>}</View>}</ScrollView></View></View></Modal>;
}