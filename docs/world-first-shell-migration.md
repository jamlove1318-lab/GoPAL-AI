# World-first shell migration

Status: planning checkpoint.

Current main still exposes the legacy Sanctuary dashboard through `HomeScreen`. The intended architecture is for Emerald Valley / `LivingValleyScreen` to be the actual home experience. This migration should remove the legacy dashboard from primary navigation without deleting it until dependency validation is complete.
