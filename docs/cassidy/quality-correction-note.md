This branch hardens Cassidy production acceptance and keeps the existing automation architecture. It blocks the former low-poly structural false positive, validates real LOD geometry, performs GLB round-trip validation, fixes package expression enumeration, binds job-specific checkpoints, improves rig weighting and animation differentiation, restores the canonical palette, and enables safe Git synchronization by default.