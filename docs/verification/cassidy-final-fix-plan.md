# Cassidy Final Verification Fixes

This branch contains targeted fixes identified during offline/static verification.

Verified targets:
- learner-aware mini-game history fallback
- canonical Cassidy home-world constant
- canonical mini-game location policy
- learner-scoped World continuity
- Yarn-based CI cache/install consistency

A full dependency-resolving TypeScript build still requires a network-enabled CI runner.
